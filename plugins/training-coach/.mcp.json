{
  "mcpServers": {
    "training": {
      "type": "http",
      "url": "${TRAINING_MCP_URL:-https://alephknot-training.fly.dev/mcp}",
      "headers": {
        "Authorization": "Bearer ${TRAINING_API_KEY}"
      }
    }
  }
}
