{
  "name": "training-coach",
  "version": "0.2.0",
  "description": "Coach persona and live tools for your training plan: reads your sessions, logs and analytics, and rewrites upcoming training with your confirmation.",
  "author": {
    "name": "Training app"
  },
  "keywords": ["training", "coaching", "strength", "running", "gymnastics", "fitness"],
  "userConfig": {
    "api_key": {
      "type": "string",
      "title": "Training app API key",
      "description": "Copy it from the app's Settings screen. Stored in your system keychain, never shown again.",
      "sensitive": true,
      "required": true
    },
    "server_url": {
      "type": "string",
      "title": "Server address",
      "description": "Only change this if you run your own server. Use its base URL with /mcp on the end.",
      "default": "https://alephknot-training.fly.dev/mcp",
      "required": false
    }
  }
}
